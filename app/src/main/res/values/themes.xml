<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash: dark maritime blue; title fades in in Compose -->
    <style name="Theme.MarineWeather" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.MarineWeather.Splash" parent="Theme.SplashScreen">
        <item name="android:colorBackground">@color/splash_background</item>
        <item name="android:windowBackground">@drawable/splash_window_background</item>
        <item name="windowSplashScreenBackground">@drawable/splash_window_background</item>
        <!-- Transparent — real icon only in Compose (no solid/masked square). -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_transparent_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="windowSplashScreenAnimationDuration">0</item>
        <item name="postSplashScreenTheme">@style/Theme.MarineWeather</item>
    </style>
</resources>
